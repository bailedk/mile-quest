{
  "name": "@mile-quest/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Mile Quest",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "echo 'Shared package linting not configured yet'",
    "test": "echo 'Shared package tests not configured yet'"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "zod": "^3.22.0"
  }
}